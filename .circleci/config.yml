version: 2.1

orbs:
  node: circleci/node@4.1

jobs:
  build:
    docker:
      - image: cimg/node:15.1
    steps:
      - checkout
      - node/install-packages
      - run: npm run buil

workflows:
  build: 
    jobs:
      - build